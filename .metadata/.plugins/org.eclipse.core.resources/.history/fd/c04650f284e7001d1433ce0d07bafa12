package com.stockControl.model;

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name = "tipo")
public enum Tipo {
	Inclusão,
	Exclusão,
	Alteração,
	Entrada,
	Saída
}